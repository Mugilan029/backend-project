package com.example.crudprojects.spec;

import com.example.crudprojects.entity.student;
import org.springframework.data.jpa.domain.Specification;

public class UserSpecification {



    //for paginationwithfilter
   /* public static Specification<student> nameContains(String name) {
        return (root, query, cb) ->
                name != null && !name.isEmpty() ? cb.like(cb.lower(root.get("name")), "%" + name.toLowerCase() + "%") : null;
    }
*/





}
